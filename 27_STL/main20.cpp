// Other Algorithms : min and max

#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    
    cout << "Maximum of (40, 99) is = " << max(40, 99) << endl;
    cout << "Minimum of (64, 203) is = " << min(64, 203) << endl;

    return 0;
}